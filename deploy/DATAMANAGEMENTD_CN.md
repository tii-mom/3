# 3API 数据管理守护进程说明

数据管理守护进程属于可选高级能力，主要用于本地或隔离环境的数据维护场景。

生产环境使用前必须满足：

1. 已明确守护进程需要访问的数据范围。
2. 已完成生产数据库备份。
3. 已在隔离恢复环境验证命令不会破坏用户、订单、余额、兑换码、订阅、提现和算力公司收益数据。
4. 已记录回滚方式和操作人。

如没有明确维护需求，不建议在生产环境启用额外数据管理进程。
