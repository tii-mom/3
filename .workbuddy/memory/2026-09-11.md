# 2026-09-11

## 上线前风险评估（用户问：后端数据动没动？先部署还是先做优化？）

### 结论 1：数据库改动是安全的（已逐条核实）
`backend/migrations/` 新增 203/204/205/206，逐个读过，**全部是纯加法**：
- 203 `shop_orders.fulfillment_note`
- 204 `usage_logs` / `ops_error_logs` 加 attempt_count、failover_count
- 205 `balance_vouchers` 加幂等字段 + 唯一索引
- 206 商城公开销售：`shop_products` 加 fulfillment_mode 等 5 列 + CHECK 约束；
  `shop_orders` 加 order_no/guest_token/delivery_payload 等 8 列 + 3 个索引；
  INSERT 一个系统游客账号 `guest@internal.3api.invalid`
- 全部 `ADD COLUMN IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS`，**零 DROP/DELETE/UPDATE**
- 206 的 INSERT 带 `WHERE NOT EXISTS` 保护，且 password_hash 是非法值
  `'nologin:system-guest-account-do-not-delete'`（不可登录，仅承载免登录订单）
→ **不碰任何现有用户数据**，可放心部署。

### 结论 2：路由接线是完整的（我一度误报，已纠正）
`backend/internal/server/router.go:130` 已注册：
`routes.RegisterPublicShopRoutes(v1, h.PublicShop)`，无需鉴权的 6 个端点：
`/public/shop/{products,banners,payment-methods}`
`POST /public/shop/orders`、`POST /public/shop/orders/delivery`、`GET /public/shop/orders/lookup`
后台解密端点 `GET /admin/shop/orders/:id/delivery` 有 adminAuth + 审计。
**没有上线阻塞 Bug。**

### 结论 3：真正的风险不在数据库，在「未提交 + 未验证」的代码
- `git status` 有 **41 个后端文件未提交**（其中 39 个是 9/10 上午的商城 + AI 功能，
  仅 2 个 `internal/web/embed_on.go`、`embed_test.go` 是 9/10 晚我做的 SEO 改动）
- 网关核心链路 6 文件共 +104/-17 行语义改动（Antigravity 混合调度放宽、
  模型 ID 跳过通配符、Codex CLI 图片生成桥接），**这些会改变所有 API 转发的行为**
- 这些改动**从未在真实环境跑过**
→ 部署前必须：① 备份数据库 ② 提交代码 ③ 部署 ④ 真实下单验证

### 给用户的排序建议（已答复）
先部署验证，不要先做优化。三项优化推到上线后：
Product/Offer 结构化数据（需要真实商品价格数据，上线后做更准）、
og:image 按落地页拆分（纯美化，不急）、
P3 SettingsView 拆分（部署前做纯重构 = 主动增加变量，最差时机）。

### 教训（我自己）
用 `grep ... | head -N` 判断「某函数是否被调用」会漏判——
本次 router.go:130 被 `head -6` 截断，导致我误报「路由没接线」。
**判断符号是否存在，要全仓 grep 且不加 head 截断，或先 grep -c 计数。**

## 部署执行（08:28-09:0x）

### 部署架构（重要，下次直接照做）
- 仓库 `tii-mom/3` 是 `Wei-Shaw/sub2api` 的 **fork**。
  **`gh` 默认跟随 upstream**，所有 gh 命令必须加 `--repo tii-mom/3`，否则查错仓库（我踩过）。
- `pages-deploy.yml`：push main 且 `paths` 命中 `frontend/**` → **自动部署前端到 Cloudflare Pages**（立即生效）
- `deploy.yml` 两个 job：
  - `build-and-push`：`if: event_name == 'push'` → 只构建镜像推 GHCR，**不部署**
  - `deploy-to-vps`：`if: event_name == 'workflow_dispatch'` + `environment: production` → 手动部署后端
- `production-preflight.yml`：手动 dispatch，做生产 DB 备份 + 隔离恢复验证，
  需 `-f image_digest=sha256:... -f confirmation=BACKUP_AND_RESTORE_ONLY`

### ⚠️ 部署顺序陷阱（务必遵守）
前端首页**强依赖** `/public/shop/products`、`/public/shop/banners`。
若一次性 push（前后端一起），新首页立即上线但后端还是旧的 → **首页商品区直接挂**。
**正确顺序**：
1. 只提交并 push `backend/`（不触发 pages-deploy，因为 paths 只监听 frontend/**）
2. CI 通过后手动 dispatch 部署后端到 VPS
3. 再提交并 push `frontend/`（此时后端已就位，前端无缝上线）
注意：不能在同一个 commit 链里做——merge commit 的父链会带上前端 commit，
一次 push 必然把前端也推上去。所以要 `git reset --soft origin/main` 后**分两次 commit + 分两次 push**。

### 本次执行结果
- commit `0d2c95f3e` feat(shop)：后端（公开商城 + 网关 + title 销售文案）
- commit `5945756ee` fix(shop)：修 `shop_guest_service.go` 的 `strings.Builder` 返回值未检查
  （golangci-lint errcheck 报错，是上午商城代码遗留，非本轮 SEO 改动引入）
- CI 最终态（5945756ee）：
  ✅ shell / golangci-lint / test(Go 单测) / build-and-push / frontend
  ❌ backend-security + frontend-security —— **既存问题，与本次改动无关**：
     govulncheck 报 Go 标准库漏洞 GO-2026-6218(net/url)、GO-2026-6090(crypto/tls)、
     GO-2026-6089(net/http)，当前 go1.26.5，**需升级到 go1.26.6 才能转绿**
- 最新镜像 digest：`sha256:c082915058d35686f10770a2c659c5100b2ea7808c9a79a1a369e169e77a4d7a`
- 已触发 production-preflight（DB 备份）run 34548523490

### 前端改动尚未提交（等后端部署后再推）
`frontend/` 48 + `.workbuddy/memory` 3 个文件仍在工作区，等后端上线后再 commit + push。

## 部署完成（09:0x 全部上线）

### 域名架构（重要，别再搞混）
- `3api.shop` = **前端**（Cloudflare Pages，纯静态）。push main 改 `frontend/**` 自动部署。
- `api.3api.shop` = **后端**（VPS + Go）。`/api/v1/...` 全走这里。
- 直接用 `3api.shop/api/v1/...` 会拿到 SPA fallback 的 HTML（我一开始测错过）。
- 前端 `src/api/url.ts` 里硬编码 fallback `https://api.3api.shop/api/v1`。

### 部署执行结果
- 备份：run 34548523490 ✅（Back up production and validate isolated restore 全 success）
  - 注：`production` 环境审批 **我的 gh token 无权批准**（current_user_can_approve=false），
    需用户自己在网页点。等待计时器 5 分钟。
- 后端部署：run 34549060479 ✅（Deploy immutable image to VPS success）
  digest `sha256:c082915058d35686f10770a2c659c5100b2ea7808c9a79a1a369e169e77a4d7a`
- 前端部署：run 34549650408 ✅（Cloudflare Pages）
- 提交：`0d2c95f3e`(后端) → `5945756ee`(lint 修复) → `73ce76ea2`(前端)

### 线上验证（全部通过）
- title ✅ `3API - ChatGPT Plus/Pro 代充值与成品号独享账号`
- description ✅ 销售文案；og:image ✅ `https://3api.shop/og-home.png`（HTTP 200, 160KB）
- twitter:card ✅ summary_large_image；FAQPage JSON-LD ✅
- sitemap.xml ✅ 200
- `/api/v1/public/shop/products` ✅ 返回 12 个真实商品，
  **且 fulfillment_mode/delivery_form_hint/badge_text 等 206 新字段已生效** → migration 执行成功、真实数据完好

### ⚠️ 遗留待办（用户操作）
1. **12 个商品 fulfillment_mode 全是 manual**，需到后台逐个改：
   - GPT 充值类（id 2 GPT Plus / 4 GPT 20美元 / 5 GPT 100美金 / 11 双人拼车 / 12 四人拼车）→ `session_topup`
   - id 1 ChatGPT 成品号 → `account_delivery`
   - 租号类 → `rental`
   - 其余（Gemini / X蓝V / Codex / 银行卡）按实际情况定
   不改的话下单后走人工交付，无法自动发 Session/发号。
2. 填写后台「售后联系」设置。
3. 真机走一遍下单全链路（首页选商品→支付→提交 Session→后台查单）。
4. 重点回归 **API 转发**（网关改了 Antigravity 混合调度 / Codex 图片桥接，影响所有在用用户）。
5. 到 Search Console 提交 `https://3api.shop/sitemap.xml`。
6. 升级 Go 到 1.26.6（当前 1.26.5，govulncheck 报 3 个标准库漏洞，security job 红）。
7. 下次部署后端时会把新前端 embed 进去；目前 VPS 上后端 embed 的仍是旧前端
   （不影响 3api.shop 主站，但直接访问 api.3api.shop 会看到旧版）。

## 第二轮：首页商品分组 + 商城完善 + 视觉重构（10:06-10:5x）

### 需求（用户提的 4 点）
1. 首页看不到商品（后台已有商品）
2. 首页商品要分类（gpt代充 / gpt成品号 / X蓝V / gemini会员）
3. 商城要完善（商品介绍、图片展示栏）
4. 调 skill 优化首页：减文字、加动态背景（低内存）、优化排版

### 根因定位（重要）
- **问题 1 的真正原因不是前端过滤，是后台存不进去**：
  `backend/internal/handler/admin/shop_handler.go` 的 `shopProductRequest` 只有 11 个基础字段，
  迁移 206 新增的 `fulfillment_mode/delivery_form_hint/badge_text/spec_label/highlight`
  在 `toServiceInput()` 里被整体丢弃 → 后台表单填了也存不进去 → 12 个商品全是默认 manual。
  （上次我以为只是"没配"，其实是"配不了"）
- 问题 2：商品模型根本没有品类字段，`fulfillment_mode` 是"怎么交付"，不是"卖什么"，两个维度

### 本轮改动
**后端**
- migration 207（纯加法）：`shop_products` 加 `category`（6 值 + CHECK）与 `gallery_json`，加 (category, sort_order, id) 索引
- `shop_service.go`：常量 + `isKnownCategory` + `normalizeGallery`（去空去重截断 6 条）+ `encode/decodeGalleryJSON`（脏数据退化为空数组）
- `shop_handler.go`：补全 206/207 字段透传（这是本次最关键的修复）

**前端**
- 新建 `src/constants/shop.ts` 作为品类枚举**单一来源**（此前 publicShop.ts / shop.ts 已各写一份，三处重复）
- 首页由「交付模式 tab」改为「品类分组 + 锚点导航」，空品类不渲染
- PlanCard：hover 切图、`N 图`角标、介绍截两行（原来行数不同导致价格行错位）
- OrderSheet：主图 + 缩略图条 + 多行介绍
- 视觉：两团纯 CSS 渐变光斑（34s/44s 极缓呼吸，只动 opacity/transform，0 JS）
- 砍「服务保障」6 卡 + 「结束语」段落；对比表 → 三列卡；文案 832 → 633 字（-24%）

### 提交
- `a25bfc0f3` feat(shop) 后端（CI 全绿，只有既存的 security 红）
- `5962edb25` feat(home) 前端（**未推送**，等后端先上线）

### 本地 Go 工具链（重要，省事）
本机原本无 Go。已装到 `/Users/yu1/.workbuddy/binaries/go`（go1.26.5 darwin/arm64）。
- **`gofmt` 可用**，本轮靠它发现并修正了两处 struct 对齐问题（新增 `[]string` 会把整列宽度撑大）
- **`go build` 不可用**：该工具链的 stdlib 源有重复声明（`internal/strconv/uscale.go` 与
  `ftoafixed.go` 都定义 `uint64pow10`，且 `internal/runtime/atomic/linkname.go` 报
  `//go:linknamestd is not allowed in the standard library`）。删掉 uscale.go 后仍失败，是工具链本身的问题。
  → **编译校验只能靠 CI**

### ⚠️ 未完成：部署（本机网络中断）
10:50 起本机对外网络全断（api.github.com / 3api.shop / api.3api.shop 全部 000，沙箱内外都一样），
所以后端部署与前端推送都没做。网络恢复后按顺序：
1. 取 `a25bfc0f3` 的 build-and-push digest
2. `gh workflow run production-preflight.yml -f image_digest=... -f confirmation=BACKUP_AND_RESTORE_ONLY` → 等用户批 `production`
3. `gh workflow run deploy.yml -f image_digest=... -f confirmation=DEPLOY_PREFLIGHTED_DIGEST` → 再等用户批
4. `git push origin main` 推前端 `5962edb25`（dist 未跟踪，由 CI 构建时 embed）
注意：deploy.yml 只校验 confirmation 字符串与 digest 格式，不绑定具体 preflight run。

### 预览截图工作流
`output/preview/`（已被 .gitignore 覆盖）下有 `mock-server.mjs` / `shot.mjs` / `shot-sections.mjs`，
可复用。产物在 `output/`：整页 5 张 + 元素级 8 张。

## agent.space 竞品拆解（11:20-，用户要求照此风格重做首页，但主业是 GPT）

### 拆解来源
`/tmp/agent_space.html` + `/tmp/site.css` + `/tmp/root.css`（curl 抓的静态资源），截图在 /tmp/agent-*.png。
注：当前模型读不了图，所有视觉结论均来自 HTML/CSS 文本分析，不是看图得出的。

### agent.space 视觉配方（可直接复用的手法）
首屏是 `site-immersive-shell`（**高 218svh**），5 层叠加：
1. `site-immersive-aurora` z0：3 个 radial-gradient（柠檬 `#e3ffb5` / 青 `#7fe1ee` / 紫 `#a7a0ff`）
   + `filter: blur(54px) saturate(112%)`，居中，是全站唯一色彩来源
2. `site-immersive-lens` z1：椭圆 `border-radius: 48%/42%` + 三重 inset box-shadow + mask 渐变，制造"往里看"景深
3. `site-immersive-world` z2：`perspective: 1100px; transform-style: preserve-3d`
4. `site-immersive-orb`：`backdrop-filter: blur(20px) saturate(138%)` + 30%/18% 高光 + 9s 浮动动画
5. `site-immersive-grain` z5：SVG `feTurbulence` 噪声，opacity .13，`mix-blend-mode: multiply`

其他：字体 Geist Variable / Geist Mono；`site-marquee` 品牌跑马灯（36s linear infinite + 两端 mask 渐隐）；
`site-reveal-group/item` 滚动入场（只做入场，无持续动画）；章节用 `01 / 02 / ...` 编号 eyebrow。

### 主题与色板
默认**深色**：`--background: oklch(14% .004 264)`，前景 `oklch(96.5% .002 264)`；
另有 `#050505`、`#101b2f` 变体。品牌蓝 `#123fd2`/`#118cff`/`#8fb6ff`，强调橙红 `#ef2000`。

### 我的判断（哪些不能照抄）
- **218svh 首屏不能抄**：我们是交易页，首屏太高会把商品推到折叠线下，直接掉转化
- **纯深色要谨慎**：支付页要填手机号/邮箱，深色信任度偏低 → 建议「上深下浅」双段式
- **`backdrop-filter` 要慎用**：多层 blur 在中低端安卓掉帧，与用户上一轮「低内存」要求冲突 → 用渐变+内阴影模拟玻璃
- **转化路径不同**：它是 SaaS「先注册后付费」，我们是「看到即下单」，商品卡+下单抽屉必须是视觉重心

### 待用户拍板（尚未执行）
1. 深浅：上深下浅 / 全深 / 全浅+开关
2. 极光配色：暖橙系（品牌一致）/ 多彩青紫橙（像 agent.space）
3. 首屏高度
4. 悬浮元素：商品预览卡 / logo 球 / 不做

## 第三轮：agent.space 风格首页落地 + 一次性上线（12:0x-12:2x）

### 用户拍板
- 上深下浅（采纳）
- 悬浮元素 = 商品预览卡
- 照抄 agent.space 顶部折扣条，点击后进入购买环节
- **本轮视觉与上一轮合并成一版一次性上线**（省一轮 production 审批）

### 落地内容
**新增组件**
- `components/home/HeroPromo.vue`：促销条。近黑底 + 三处径向光晕（蓝 `#0099ff` / 琥珀 `#ffb100` / 洋红 `#ff1fb8`）+ 5s 扫光，点击 emit `action`
- `components/home/HeroFloatCards.vue`：3 张真实商品卡，3D 错位（-5deg/4deg/-2deg）+ 9s `float-bob`，
  玻璃质感用「径向高光 + 半透明渐变 + 内阴影」**模拟**（刻意不用 `backdrop-filter`，中低端安卓会掉帧），
  窄屏（≤900px）退化为横向滑动条。点击 emit `select` → 打开下单抽屉
- `composables/useRevealOnScroll.ts`：`v-reveal` 指令。IntersectionObserver，触发一次即 unobserve；
  reduced-motion / 不支持 IO 时直接置为可见（绝不把内容藏起来）
- `utils/heroPromo.ts`：`planHeroPromo()` 首屏选品规则

**HomeView 首屏改成五层纯 CSS 叠加**
极光四光斑（暖橙 `rgba(255,122,69,.52)` / 蓝紫 `rgba(124,92,255,.48)` / 青 `rgba(34,211,238,.4)` /
暖粉 `rgba(255,77,109,.34)`，34-44s 呼吸）→ 椭圆透镜（`border-radius:48%/42%` + 三重 inset shadow，
mask 上下都要淡到 0，否则促销条上方会留一道白线）→ 内容 → feTurbulence 颗粒（opacity .16，overlay）
→ 悬浮卡。动画只动 opacity/transform，0 JS / 0 canvas / 0 图片。
深色段与浅色段之间加跑马灯（36s，轨道**不能有 gap**，间距要放进 item 的 padding-right，
否则两份列表宽度不等、-50% 循环会跳）。

**浅色转化段**：加 `01-04` 分段编号 eyebrow + `v-reveal` 滚动入场；锚点加 `scroll-margin-top:76px`
（吸顶导航 60px，不加会把标题盖住）；`html:has(.sales-home){scroll-behavior:smooth}`。

### ⚠️ 促销条不允许写死折扣（重要产品原则）
初版文案是我照抄的「新客首单立减 ¥20」——**后台根本没有这个优惠规则，属于虚假宣传**。
改为 `planHeroPromo()` 纯函数（`utils/heroPromo.ts` + 6 个单测）：
1. 只主推后台勾了「**推荐位**」（`highlight`）的商品 → 选品权交回店主；
2. 推荐位商品有划线原价 → 显示「省 ¥X」，金额来自真实数据；
3. **没有任何推荐位 → 退化成不含商品名的通用文案**，badge 用全站最低价「最低 ¥X 起」。

第 3 条是被线上实测逼出来的：按「折扣最大」自动选品时，选中了
「Gemini 12个月（无质保）· 省 ¥1,162」放在首屏最响的位置——名字带负面词，反而伤转化。
（另：线上 12 个商品 `highlight` **全是 false**，所以第一版规则形同虚设。）

### 部署执行（网络已恢复）
1. push `f250d43ec` → CI ✅ / Pages ✅ / build-and-push ✅（Security Scan ❌ 既存问题）/ Pages 资源哈希与本地构建一致
2. production-preflight run `34560928190` ✅（DB 备份 + 隔离恢复验证）
3. deploy.yml dispatch run `34561410044` → **waiting，等用户点 production 审批**
   digest = `sha256:41432dd9bbbcb048536acfeba08cdf9388d7c0f4840e6ae6062160393c375b2b`（= `f250d43ec`）
4. 发现促销条选品问题 → 补 commit `17e2e76eb` 并 push（**前端-only，不影响已审批的 digest**）
   → Pages 已上线修正版；VPS 镜像会停在 `f250d43ec`（内嵌前端少这一个 commit，
   但 `api.3api.shop` 服务的是管理台，不渲染 HomeView，零影响）

### 域名架构补充（本轮实测确认）
- `3api.shop` = Cloudflare Pages，返回 **5.3KB 静态 SEO shell**（title 销售文案）
- `api.3api.shop/` 与 `/admin` 都返回 **406KB SPA**（内嵌在 Go 镜像里）→ 管理台在这
- 所以「首页改动」只需 Pages；「后端/migration」才需要 preflight+deploy 那套

### ⚠️ 线上现状（部署前）
`/api/v1/public/shop/products` 返回的 12 个商品**没有 `category` 字段**
（migration 207 还没上 VPS）→ 首页 12 个商品全挤在「其他服务」一组，分类导航不显示。
**这正是用户问题 2「首页商品要分类」还没真正生效的原因，等 deploy 审批后即修复。**

### gh 多远程的坑（新）
本仓库有两个 remote（`origin`=tii-mom/3，`upstream`=Wei-Shaw/sub2api），
`gh` 会因此报 `No default remote repository has been set`。
**已执行 `gh repo set-default tii-mom/3` 修好**，后续 gh 命令不用再手动加 `--repo`。

### 验证方式（本轮定型，可复用）
- `output/preview/shot-scroll.mjs`：先分步滚到底触发所有 `v-reveal`，再整页截图 + 逐块截图 +
  统计「未触发的 reveal 元素数」（必须为 0）。**教训**：`fullPage:true` 不滚动，
  滚动入场元素会全白，直接看会误判成「页面挂了」
- `output/preview/check-hero-actions.mjs`：首屏交互验收（悬浮卡/促销条能否打开抽屉、
  锚点跳转是否被吸顶导航遮挡、有无运行时错误）
- `output/preview/shot-live.mjs`：线上站点验收（真实 3api.shop，不走 mock）
- **Playwright stability 陷阱**：带无限浮动动画的元素 `click()` 永远超时（element is not stable），
  必须 `click({ force: true })`；抽屉只提供右上角关闭按钮，**没有 Esc 监听**，关抽屉要点 `.sheet__close`
- mock 数据要覆盖分支：`NO_HIGHLIGHT=1` 复现线上「一个推荐位都没勾」的场景
- Chromium 路径：`~/Library/Caches/ms-playwright/chromium-1228/chrome-mac-arm64/...`
  （**是 arm64 子目录，不是 `chrome-mac`**）
